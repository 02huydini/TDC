// TileTint is no longer used. Path highlighting is now done with LineRenderers
// (PathHighlighter for Shift-all-paths, EnemyHoverInfo for per-enemy-path).
// Kept as an empty stub so any .meta reference does not produce a missing-script
// error on scenes that have not been resaved yet.
